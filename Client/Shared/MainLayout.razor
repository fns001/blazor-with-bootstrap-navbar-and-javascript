@inherits LayoutComponentBase

<BSNav FixedTop=FixedTop />

<div class="main@(FixedTop ? " with-fixed-top" : "")">
    <div class="container mt-3">
        @Body
    </div>
</div>

@code {
    private const bool FixedTop = true;
}